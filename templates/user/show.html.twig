{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <div class="card">
        <h4 class="text-center">Utilisateur</h4>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ user.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ user.firstName }}</td>
                </tr>
                <tr>
                    <th>Prénom</th>
                    <td>{{ user.lastName }}</td>
                </tr>
                <tr>
                    <th>Adresse email</th>
                    <td>{{ user.email }}</td>
                </tr>
                <tr>
                    <th>Roles</th>
                    <td>{{ user.role }}</td>
                </tr>
            </tbody>
        </table>
    </div>
    <div class="d-flex justify-content-center mt-3">
        <a href="{{ path('user_index') }}" class="btn btn-secondary mr-2">Retour</a>
        {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
